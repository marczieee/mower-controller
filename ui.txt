          {/* Automatic Mode Toggle */}
          <button
            onClick={() => setIsAutoMode(!isAutoMode)}
            className={`group relative flex items-center gap-4 p-4 rounded-2xl border transition-all duration-500 ${
              isAutoMode
                ? "bg-linear-to-br from-cyan-600/30 to-cyan-800/30 border-cyan-500/50 shadow-lg shadow-cyan-500/20"
                : "bg-linear-to-br from-slate-700/50 to-slate-800/50 border-slate-600/50 hover:border-slate-500/50"
            }`}
          >
            <div
              className={`p-3 rounded-xl transition-all duration-500 ${
                isAutoMode
                  ? "bg-cyan-500/30 text-cyan-400"
                  : "bg-slate-600/50 text-slate-400 group-hover:text-slate-300"
              }`}
            >
              <Cpu className="w-6 h-6" />
            </div>
            <div className="flex-1 text-left">
              <span className="block text-sm font-semibold text-slate-200">
                Auto Mode
              </span>
              <span
                className={`text-xs ${
                  isAutoMode ? "text-cyan-400" : "text-slate-500"
                }`}
              >
                {isAutoMode ? "Enabled" : "Disabled"}
              </span>
            </div>
            {/* Toggle Indicator */}
            <div
              className={`w-12 h-6 rounded-full p-1 transition-all duration-300 ${
                isAutoMode ? "bg-cyan-500" : "bg-slate-600"
              }`}
            >
              <div
                className={`w-4 h-4 rounded-full bg-white shadow-md transition-transform duration-300 ${
                  isAutoMode ? "translate-x-6" : "translate-x-0"
                }`}
              />
            </div>
          </button>


///////////////////////////////////////////////////////////////////////////////
          //////////////////////////////////////////////////////

               <div className="flex flex-col gap-2 items-center justify-center">
            <button
              onClick={() => handleCutterToggle(!isCutterOn)}
              className={`group w-fit relative flex items-center gap-4 p-2 rounded-2xl border transition-all duration-500 ${
                isCutterOn
                  ? "bg-linear-to-br from-emerald-600/30 to-emerald-800/30 border-emerald-500/50 shadow-lg shadow-emerald-500/20"
                  : "bg-linear-to-br from-slate-700/50 to-slate-800/50 border-slate-600/50 hover:border-slate-500/50"
              }`}
            >
              <div
                className={`p-2 rounded-xl transition-all duration-500 ${
                  isCutterOn
                    ? "bg-emerald-500/30 text-emerald-400"
                    : "bg-slate-600/50 text-slate-400 group-hover:text-slate-300"
                }`}
              >
                <Fan
                  className={`w-6 h-6 ${isCutterOn ? "scale-135 animate-spin transition-transform" : "animate-pulse transition-transform"}`}
                />
              </div>
            </button>

            <button
              onClick={() => handleAutoModeToggle(!isAutoMode)}
              className={`group w-fit relative flex items-center gap-4 p-2 rounded-2xl border transition-all duration-500 ${
                isAutoMode
                  ? "bg-linear-to-br from-blue-600/30 to-emerald-800/30 border-blue-500/50 shadow-lg shadow-blue-500/20"
                  : "bg-linear-to-br from-slate-700/50 to-slate-800/50 border-slate-600/50 hover:border-slate-500/50"
              }`}
            >
              <div
                className={`p-2 rounded-xl transition-all duration-500 ${
                  isAutoMode
                    ? "bg-blue-500/30 text-sky-400"
                    : "bg-slate-600/50 text-slate-400 group-hover:text-slate-300"
                }`}
              >
                <CircleArrowUp
                  className={`w-6 h-6 ${isAutoMode ? "scale-100 animate-ping transition-transform" : "animate-pulse transition-transform"}`}
                />
              </div>
            </button>
          </div>


          //////////////////////////////////////////////////////////////////
           {/* Divider */}
        <div className="my-8 h-px bg-linear-to-r from-transparent via-slate-600 to-transparent" />
        {/* Toggle Buttons Section */}
        <div className="grid grid-cols-2 gap-6">
          {/* Grass Cutter Toggle */}
        </div>
        {/* Status Indicator */}
        <div className="mt-0 flex items-center justify-center gap-2">
          <div
            className={`w-2 h-2 rounded-full ${
              isCutterOn || isAutoMode
                ? "bg-emerald-500 animate-pulse"
                : "bg-slate-600"
            }`}
          />
          <span className="text-xs text-slate-500">
            {isCutterOn || isAutoMode ? "System Active" : "System Standby"}
          </span>